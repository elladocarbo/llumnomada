import type { Settings } from '../lib/types.mts';
import { DEFAULT_CATEGORIES } from '../lib/config.mts';

export const SEED_SETTINGS: Settings = {
  instagram: '',
  portrait: '',
  categories: DEFAULT_CATEGORIES,
};
