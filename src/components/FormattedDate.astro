---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('ca-ES', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
